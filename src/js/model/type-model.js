class TypeModel {
    constructor (CONFIG){
        this.typeList = CONFIG
        this.modelList = this.getModelList(this.typeList)
    }
    getModelList(typeList){
        let modelList = []
        typeList.map((item)=>{
            modelList.push()
        })
    }
}
